
const AuthenticationService = {
  signIn: async () => {
    try {

    } catch(err) {
      const error = err as Error;
      console.log(error);
    }
  },
  signOut: async () => {
    try {
      
    } catch(err) {
      const error = err as Error;
      console.log(error);
    }
  }
}

export default AuthenticationService;